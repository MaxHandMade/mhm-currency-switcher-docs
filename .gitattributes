# Generated by scripts/generate-placement-page.js, which writes LF. With
# core.autocrlf=true (the Windows default) git checks these out as CRLF, so every
# local run of the placement gate left both files "modified" with no content
# change. Pinning them to LF on checkout keeps the working tree clean after the
# generator runs. CI runs on Linux and is unaffected.
docs/yerlestirme.md text eol=lf
i18n/tr/docusaurus-plugin-content-docs/current/yerlestirme.md text eol=lf
