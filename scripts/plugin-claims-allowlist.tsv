# Tokens a page names ON PURPOSE, because the sentence is about the name.
#
# NOT "findings we tolerate". Sweeping one of these would make the sentence
# false — worse than the stale token it fixes.
#
# 🔴 AN ENTRY IS A CLAIM ABOUT ONE SENTENCE, NOT ABOUT A FILE. That is why the
#    third column exists: the token is spared only on lines matching it.
#    A (file, token) list would hide a second, stale occurrence in the same file.
#
# path	token	anchor (LITERAL substring — not a regex; metacharacters match themselves)
#
# An entry earns its place only when the gate would otherwise report the
# sentence. The rest-api rows were the first real entries: they spare the
# "Removed in 2.1.0" note's `/mhmcs/v1/rates` mention. They were INERT while
# the anchor was v2.0.0, where /rates is a live route and source_has() found
# it with or without the pardon. They went live with the 2.1.0 anchor bump —
# /rates dropped out of source, and without them the sentence naming the
# removed route would itself have become a false-positive finding.
# The 2.1.0 sweep added the two CLI rows below. The `wp mhm-cs` namespace was
# renamed to `wp mhmcs`, and both wp-cli pages say so in a rename note that has
# to spell the dead command for the sentence to mean anything. No CSS row was
# needed: those notes name only the `mhm-cs-` prefix, which the plugin readme
# still carries in its own changelog.

docs/rest-api.md	/mhmcs/v1/rates	Removed in 2.1.0
i18n/tr/docusaurus-plugin-content-docs/current/rest-api.md	/mhmcs/v1/rates	2.1.0'da kaldırıldı
docs/wp-cli.md	wp mhm-cs rates-sync	Renamed in 2.1.0
i18n/tr/docusaurus-plugin-content-docs/current/wp-cli.md	wp mhm-cs rates-sync	2.1.0'da yeniden adlandırıldı
